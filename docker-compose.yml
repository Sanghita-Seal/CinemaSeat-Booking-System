version: "3.9"

services:
  postgresdb:
    image: postgres:17
    container_name: bookmyticket_db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sql_class_2_db
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: